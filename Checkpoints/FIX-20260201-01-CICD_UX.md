# Checkpoint: Correcciones de CI/CD y UX Admin
**ID:** FIX-20260201-01
**Fecha:** 2026-02-01
**Autor:** @SOFIA (Code) + @INTEGRA (Doc)

## 🚀 Resumen de Cambios
Sesión enfocada en resolver problemas de integración continua (GitHub Actions/Vercel) y mejorar la usabilidad para administradores mediante controles rápidos de configuración.

## 🛠️ Correcciones Técnicas (CI/CD)
1.  **Vercel Build Fix:**
    - Se agregó script `build:web` (`vite build`) para evitar que Vercel intente compilar binarios de Electron.
    - Se eliminaron imports directos de `electron` en componentes de React que rompían el build web.
    - Se corrigió error de tipado estricto (`null check`) en `main.ts` al inicializar `VirtualPrinterServer`.

2.  **GitHub Actions Fix:**
    - Se sincronizó `pnpm-lock.yaml` con `package.json` (faltaba `@types/uuid`).
    - Se corrigió importación circular/fanntasma de `AppConfig` en `configIPC.ts`.
    - Se eliminó la restricción `--frozen-lockfile` en el workflow de Windows para permitir regeneración de lockfile cross-platform si es necesario.

## ✨ Mejoras de UX (Admin)
1.  **Selector de Modo Rápido:**
    - Se implementó un botón toggle en la barra superior (`HeaderBar`).
    - **Visible solo para:** `ADMIN` y `SUPER_ADMIN`.
    - **Función:** Permite alternar entre modo `DEMO` (Simulador) y `PRODUCTION` (Hardware Real) sin reiniciar la app ni editar archivos.
    - **Indicadores Visuales:** 🟦 Azul para Demo, 🟥 Rojo parpadeante para Producción.

## 📦 Estado del Repositorio
- Rama: `master`
- Último Commit: `FIX-20260201-05`
- Estado: **Estable** y listo para pruebas de campo.

---
*Fin de sesión. Todo sincronizado.*
